# Backend (Flask)

This folder contains a minimal Flask app for inference and utilities. The app exposes `/analyze-image` which currently returns a placeholder response. Replace preprocessing and model loading with trained weights later.
